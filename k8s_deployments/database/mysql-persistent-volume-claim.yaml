apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-data-persisent-volume-claim
spec:
  volumeName: mysql-data-persisent-volume
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi